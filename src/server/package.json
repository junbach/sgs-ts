{
  "name": "sgs_server",
  "version": "1.0.0",
  "main": "index.js",
  "author": "Shaun",
  "license": "GPL-3.0-only",
  "dependencies": {
    "chalk": "^4.1.2",
    "express": "^4.17.2",
    "socket.io": "2.4.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.13",
    "@types/socket.io": "^3.0.2",
    "cross-env": "^7.0.3",
    "nodemon": "^2.0.15",
    "ts-node": "^10.4.0",
    "tsconfig": "^7.0.0",
    "tsconfig-paths": "^3.12.0",
    "typescript": "^4.5.4"
  },
  "scripts": {
    "patch": "ts-node ../pre_release.ts",
    "dev:watch": "NODE_PATH=../ nodemon",
    "dev": "NODE_PATH=../ ts-node -P ./tsconfig.server.json ./main.ts",
    "build": "NODE_PATH=../ tsc -p ./tsconfig.server.json",
    "dev:win": "cross-env NODE_PATH=../ ts-node -P ./tsconfig.server.json ./main.ts",
    "dev:win:watch": "cross-env NODE_PATH=../ nodemon",
    "build:win": "cross-env NODE_PATH=../ tsc -p ./tsconfig.server.json",
    "start": "npm run build && NODE_PATH=./build REACT_APP_DEV_MODE=prod node ./build/server/main.js",
    "start:win": "npm run build:win && cross-env NODE_PATH=./build REACT_APP_DEV_MODE=prod node ./build/server/main.js",
    "start:dev": "npm run build && NODE_PATH=./build REACT_APP_DEV_MODE=dev node ./build/server/main.js",
    "start:dev:win": "npm run build:win && cross-env NODE_PATH=./build REACT_APP_DEV_MODE=dev node ./build/server/main.js"
  }
}
